# LiDAR<->LiDAR extrinsic calibration (run on your PC, not the Jetson).
# Install: pip install -r requirements.txt
numpy>=1.24
scipy>=1.10
pyyaml>=6.0
matplotlib>=3.6
kiss-icp>=1.0      # target-less LiDAR odometry (Python API used by run_odometry.py)
rosbags>=0.9       # pure-python ROS 2 bag reader (run_odometry.py uses the ROS2_HUMBLE typestore)
small-gicp        # map-based refinement (refine_map_gicp.py)
