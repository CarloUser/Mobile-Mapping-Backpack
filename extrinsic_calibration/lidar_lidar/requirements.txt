# LiDAR<->LiDAR extrinsic calibration (run on your PC, not the Jetson).
# Install: pip install -r requirements.txt
numpy>=1.24
scipy>=1.10
pyyaml>=6.0
matplotlib>=3.6
kiss-icp>=1.0      # target-less LiDAR odometry (provides kiss_icp_pipeline CLI)
rosbags>=0.9       # pure-python ROS 2 bag reader used by KISS-ICP
